# Power BI Row Level Security (RLS) with Calculate Tables

Example Power BI project (.pbip) showing how to use DAX Calculated Tables to provide comparative data metrics across a full dataset whilst RLS is applied to the data model.
